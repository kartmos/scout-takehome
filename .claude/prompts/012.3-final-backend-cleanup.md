# Goal

Close the remaining backend-review/test-quality findings after task 012.2: make the cancelled-generation cleanup test deterministic, cover the nil `ThumbnailSvc` router guard, harden the thumbnail handler against an invalid nil service result, and remove the dead `ThumbnailResult.ETag` field. Do not change valid runtime contracts or add new features.

# Minimal context

Read `CLAUDE.md`, then inspect only:

- `TestCache_CancelledGenerationCleanup` and the already-correct leader/follower lifecycle tests;
- `NewRouter` dependency guards and their focused panic tests;
- `handleGetThumbnail`, `thumbnailService`, and handler tests;
- `thumbnail.ThumbnailResult`, `Service.Get`, and direct result construction sites.

Do not read previous prompts, README/OpenAPI, metrics/cache implementation beyond the named symbols, integration smoke, frontend, dataset, or Git history. Do not use subagents.

# Allowed files

```text
backend/internal/thumbnail/cache_test.go
backend/internal/thumbnail/service.go
backend/internal/httpapi/router_test.go
backend/internal/httpapi/photos_test.go
backend/internal/httpapi/thumbnails.go
backend/internal/httpapi/thumbnails_test.go
```

Use the actual existing file containing router dependency tests; do not duplicate the same test in two files. Modify no dependencies, configuration, metrics, cache algorithms, service lifecycle, endpoint contract, or integration code.

# Exact corrections

## 1. Deterministic cancelled-generation cleanup test

1. Fix `TestCache_CancelledGenerationCleanup`, which can return while the singleflight generation goroutine still owns/creates a temp file and race with `t.TempDir` cleanup.
2. Preserve production behavior: a cancelled caller returns promptly while shared work may continue under its independent generation lifecycle. Do not change `Cache.GetOrCreate` merely to satisfy the test.
3. Coordinate the test generator with explicit started/release/done channels, following the established leader/follower lifecycle-test pattern. After asserting caller cancellation, release generation and wait until the flight fully exits before the test returns or inspects final temp-file cleanup.
4. Use no sleeps, polling races, leaked goroutines, or broad repeated suite. The test must pass reliably under targeted repetition and `-race`.

## 2. Nil ThumbnailSvc router guard

5. Add `TestNewRouter_PanicsOnNilThumbnailSvc` beside the existing nil Repo/Storage guard tests, using otherwise valid router dependencies.
6. Assert only the required panic behavior; do not couple the test to the exact panic string or duplicate all router setup logic.

## 3. Invalid nil service result

7. Treat `(nil, nil)` from `thumbnailService.Get` and a non-nil result with `File == nil` as internal invariant failures before `defer Close` or image headers are written.
8. Return the existing safe typed 500 through `WriteError`; retain a useful internal cause for logging without exposing implementation details.
9. Add one concise table-driven handler regression for both invalid result shapes. Verify status/code and absence of panic/partial image headers. Do not broaden the public interface.

## 4. Remove dead ETag field

10. Remove `ETag` from `thumbnail.ThumbnailResult` because the handler derives the representation ETag before service/cache access for the 304 fast path and ignores the service field.
11. Remove assignments/assertions and update fake result literals. Keep the handler's single canonical `NewKey(...).Hash()` computation and existing ETag/304 behavior unchanged.
12. Do not move ETag generation into the cache, add a second method, or alter cache keys/versioning.

# Explicit non-changes

- Keep `/metrics` public as designed; deployment exposure/reverse-proxy restriction belongs to task 017.
- Keep `limitWriter` bounded by total cache budget. Validated source/output dimensions already bound JPEG encoder output; do not add a new environment variable here.
- Preserve server-bound generation context, coalesced miss semantics, low-cardinality route labels, eviction collector, strict cache keys, startup accounting, auth, metrics, and smoke behavior.

# Acceptance criteria

- Cancelled-generation cleanup test cannot race with temporary-directory removal.
- All three required router dependency guards have focused panic coverage.
- Invalid nil thumbnail results produce safe 500 responses instead of panics.
- `ThumbnailResult` has no unused ETag field and HTTP ETag behavior is unchanged.
- Focused repetition/race, full ordinary tests, vet, builds and diff check pass.

# Verification

During editing run only focused tests. After stable changes, run once:

```bash
cd backend
gofmt -w internal/thumbnail internal/httpapi
go test ./internal/thumbnail ./internal/httpapi
go test -run TestCache_CancelledGenerationCleanup -count=50 ./internal/thumbnail
go test -race ./internal/thumbnail ./internal/httpapi
go test ./...
go vet ./...
go build -o /tmp/scout-api ./cmd/api
go build -o /tmp/scout-seed ./cmd/seed
cd ..
git diff --check
git status --short
```

Do not run Docker or tagged integration. Inspect only the allowed diff and leave unrelated user changes intact.

# Final reviewer gate

After successful implementation and Codex diff inspection, start one final fresh read-only review:

```text
/clear
/backend-review
```

Backend closes only if it returns no actionable findings. Do not fix findings inside the reviewer context.

# Final report

Report the four corrections, focused repetition/race results, full verification, and exact reviewer command. Do not begin frontend work.
