# Goal

Fix the one confirmed runtime blocker left by the 017.1 review: the MinIO healthcheck renders the configured custom port but invokes `mc ready` with a raw URL, while `mc ready` requires a configured alias and deliberately rejects direct URLs. Keep this correction isolated to local Compose and its truthful documentation/reporting. Do not begin task 018 or change application behavior.

# Prerequisite and evidence

Run after task 017.1. Start with `git status --short` and preserve all existing changes.

Current local Compose renders:

```text
mc ready http://localhost:<SCOUT_MINIO_API_PORT>
```

This is not a valid readiness command. MinIO Client's `newAnonymousClient` calls `expandAlias` and explicitly returns `errInvalidAliasedURL` when the argument is a real URL without alias configuration. Official examples use `mc ready <alias>`.

Read only:

```text
CLAUDE.md
compose.yaml
README.md: local quick-start and known task-018/runtime notes only
```

Do not use subagents, add dependencies, edit application code/tests, inspect Git history, or modify dataset files.

# Required correction

1. Replace the invalid direct-URL healthcheck with a `CMD-SHELL` command that:
   - creates or updates a dedicated health alias pointing to `http://localhost:${SCOUT_MINIO_API_PORT:-9000}` after Compose interpolation;
   - reads `MINIO_ROOT_USER` and `MINIO_ROOT_PASSWORD` inside the container, with Compose escaping (`$$`) so their values are not interpolated into the rendered model;
   - runs `mc ready` against that alias;
   - suppresses alias/credential output and returns non-zero until readiness succeeds.
2. Do not rely on the image's preconfigured `local` alias because it targets the default port and does not follow a custom `SCOUT_MINIO_API_PORT`.
3. Do not put credential values in the command, URL, logs, README, or final report. Variable names are acceptable.
4. Preserve the existing pinned MinIO image, custom server address, host bindings, network alias, resource limits, and `minio-init` behavior.
5. Change README only if a statement becomes inaccurate. Do not claim runtime verification while the Docker daemon remains unavailable.

A suitable shape is an exec-list `CMD-SHELL` healthcheck whose shell body first performs a quiet `mc alias set <health-alias> <configured-url> "$MINIO_ROOT_USER" "$MINIO_ROOT_PASSWORD"`, then performs a quiet `mc ready <health-alias>`. Ensure `$` escaping is correct for the Compose and container-shell layers.

# Verification

With non-secret local/example values, run:

```bash
docker.exe compose --env-file .env -f compose.yaml config --quiet
```

Render the MinIO healthcheck twice: once with the default port and once with `SCOUT_MINIO_API_PORT=9099` set in the Windows process environment. Verify all of the following without printing credential values:

- the default rendered alias URL contains port `9000`;
- the custom rendered alias URL contains port `9099`;
- `mc ready` receives the dedicated alias, never an `http://...` target;
- credential references remain container-shell variables rather than Compose-expanded values;
- the seed service still renders `entrypoint: [scout-seed]`.

If the Docker daemon becomes available, run the narrow local MinIO start/health check with both ports. Otherwise do not fabricate execution success: static Compose rendering is the only verified result and runtime remains task 018.

Finally run:

```bash
git diff --check
git status --short
```

No backend/frontend language gate is necessary because this task must not touch their files. Do not stage, commit, push, or rewrite history.

# Acceptance criteria

- The rendered healthcheck configures a custom-port-aware alias and calls `mc ready` with that alias.
- No secret value is embedded or logged.
- Default and port-9099 Compose renders are valid.
- Existing seed entrypoint and MinIO topology remain unchanged.
- Docker runtime limitations are reported honestly and task 018 has not started.

# Final report

Report the exact healthcheck shape with credential values redacted, default/custom-port render results, seed entrypoint preservation, `docker compose config` and `git diff --check` results, and whether Docker runtime execution remains blocked.
