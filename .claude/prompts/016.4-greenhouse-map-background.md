# Goal

Replace the artificial `Bed 1`–`Bed 3` Konva decoration with the approved
top-down greenhouse image, rendered as a quiet background inside the existing
40×40 m map. Keep the image at 60% opacity and make it follow the same zoom/pan
transform as the grid and real photo markers. Preserve every accepted task-016.3
interaction and all assignment behavior.

# Current working state

- Work in the current branch and repository state. Begin with
  `git status --short`, record `HEAD`, and inspect the scoped diff before editing.
- `GreenhouseMap.tsx` already has uncommitted visual-constant cleanup. Preserve
  that work where it remains relevant; remove only constants that become unused
  with the old bed decoration.
- `.github/workflows/ci.yml` and `.github/workflows/integration.yml` may also be
  untracked user work. Do not edit, delete, stage, or otherwise disturb them.
- The accepted map already has real database `x,y` markers, a 40×40 m world,
  grid/axis labels, zoom/pan, a 3 m near filter, hidden/compact/expanded modes,
  expanded draft/apply behavior, and an accessible marker list.
- The current `Bed 1`, `Bed 2`, and `Bed 3` rectangles are schematic decoration
  only. They are not connected to API data, seed data, filtering, or geometry.
- Do not commit, push, rewrite history, add dependencies, or modify backend/API/
  OpenAPI/dataset/Compose/workflow behavior.

# Approved source asset

Use this exact generated image; do not regenerate, redraw, or substitute it:

```text
Windows:
C:\Users\artem\.codex\generated_images\019f2378-d8c6-7a53-855a-7dc420212a3e\exec-8885da98-759c-497e-80ff-4f2b0d896b24.png

WSL:
/mnt/c/Users/artem/.codex/generated_images/019f2378-d8c6-7a53-855a-7dc420212a3e/exec-8885da98-759c-497e-80ff-4f2b0d896b24.png
```

1. Verify the source exists and is a readable PNG before editing production code.
2. If neither path is accessible, stop and report `BLOCKED`; do not use a dataset
   photo, placeholder, remote URL, or newly generated approximation.
3. Copy the source unchanged to:

   ```text
   frontend/src/assets/greenhouse-map-background.png
   ```

4. Keep the source file in place. Do not recompress or convert it during this task;
   preserving the specifically approved visual is more important than speculative
   optimization.

# Context manifest

Read only:

```text
CLAUDE.md
TODO.md: Greenhouse map requirement only
frontend/src/features/map/GreenhouseMap.tsx
frontend/src/features/map/GreenhouseMap.module.css
frontend/src/features/map/mapGeometry.ts: exported geometry contract only
frontend/src/test/GreenhouseMap.test.tsx
frontend/package.json: dependencies and scripts only
README.md: greenhouse-map paragraph only
.claude/prompts/ROADMAP.md: task 016 rows and current-status paragraphs only
```

Inspect one directly connected file only if a demonstrated compile or focused-test
blocker requires it. Do not reread prior prompts, recursively inspect unrelated
folders, query the dataset, or use subagents.

# Scope

Create:

```text
frontend/src/assets/greenhouse-map-background.png
```

Modify only when necessary:

```text
frontend/src/features/map/GreenhouseMap.tsx
frontend/src/test/GreenhouseMap.test.tsx
README.md
.claude/prompts/ROADMAP.md
```

Modify `GreenhouseMap.module.css` only if a demonstrated layout issue requires a
small correction. Do not modify `mapGeometry.ts`, gallery state/query code,
dependencies, lockfiles, generated API types, backend files, dataset files,
Docker/Compose, or `.github/workflows`.

# Remove the old three-bed decoration

1. Remove the `BEDS` definition and the render loop that draws `Bed 1`, `Bed 2`,
   and `Bed 3`.
2. Remove bed-only constants, comments, and imports that become unused, including
   the Konva `Rect` import if it has no other caller.
3. Do not remove the 40×40 m grid, inner ticks, edge strokes, axis labels, metres
   legend, photo markers, selected-radius circle, or centre dot.
4. Do not reinterpret or remap any photo coordinates. The background is visual
   context only and must never become a source of geometry or filtering truth.

# Add the Konva background image

1. Import the copied asset through Vite and render it with Konva `Image`, using an
   explicit alias such as `KonvaImage` to avoid confusion with `window.Image`.
2. Load the browser image asynchronously without adding `use-image` or any other
   dependency. Keep the loader local and simple:
   - create an `HTMLImageElement` with `window.Image` inside an effect;
   - set state only after a successful `load`;
   - detach handlers and prevent stale state updates during cleanup;
   - a load failure leaves the functional map visible without the decorative
     background and does not throw or block interaction.
3. Render the background as the first visual node inside the existing transformed
   Konva `Layer`, before grid lines, labels, markers, and selection UI.
4. Fit it exactly to the world plot bounds:

   ```tsx
   x={plotLeft}
   y={plotTop}
   width={plotSizePx}
   height={plotSizePx}
   opacity={0.6}
   listening={false}
   ```

5. `opacity={0.6}` means 60% visible opacity. Do not preprocess the source pixels
   or stack a second opacity effect in CSS.
6. Because it is inside the transformed layer, the image must pan and zoom with
   the 40×40 m plot. Do not implement it as a CSS background on `stageWrapper` and
   do not place it in a fixed/untransformed layer.
7. Preserve the existing world-coordinate orientation. Do not flip, rotate, crop,
   tile, or independently scale the approved square image.
8. `listening={false}` is mandatory so the image cannot capture clicks, pointer
   events, drag gestures, or wheel interaction intended for the stage and markers.

# Preserve accepted map behavior

Do not regress:

- real database `x,y` marker placement and malformed-coordinate isolation;
- exact 40×40 m world↔stage conversion and inverse background-click math;
- bounded wheel/button zoom, pan, reset, and resize behavior;
- constant-on-screen marker sizes and marker hover/highlight styling;
- inclusive 3 m near predicate and visible selected radius;
- bounded ≤200 map query and shared class/confidence filters;
- compact immediate selection and expanded draft/apply selection;
- dialog Escape/focus-return behavior and marker-list accessibility;
- loading, error, empty, disclosure, coordinate-readout, and gallery behavior.

# Focused tests only

Update the existing `react-konva` test mock minimally to expose a testable
`KonvaImage` equivalent and mock browser-image loading deterministically. Avoid
real image decoding in jsdom.

Prove at minimum:

- `Bed 1`, `Bed 2`, and `Bed 3` are no longer rendered;
- the approved background node appears after successful loading;
- it receives the plot bounds, `opacity={0.6}`, and `listening={false}`;
- it is rendered before the grid/marker visual nodes;
- failed or incomplete image loading does not remove the stage or break map
  interaction;
- an empty-space click still reaches the existing selection path with the
  background present.

Prefer semantic mock props/test IDs over canvas pixel snapshots. Do not add
screenshots, browser automation, broad accessibility tests, duplicate geometry
tests, or assertions coupled to generated asset hashes.

# Documentation bookkeeping

1. Update only the greenhouse-map paragraph in `README.md`: replace the claim
   about three horizontal plant beds with a short statement that the real markers
   are displayed over a subdued greenhouse floor-plan background.
2. After focused verification passes, add task `016.4` to the relevant roadmap
   table/status text. Record removal of the artificial three-bed overlay, the
   approved 60%-opacity Konva background, and preserved map behavior. Do not alter
   historical task facts or claim full-suite/runtime verification.

# Acceptance criteria

- The exact approved PNG is tracked under `frontend/src/assets/`.
- No `Bed 1`, `Bed 2`, or `Bed 3` label/overlay remains in the production map.
- The image fills the 40×40 m plot at 60% opacity and moves with zoom/pan.
- Grid, labels, real photo markers, and selection indicators remain above it and
  readable.
- The image never captures map input and load failure degrades to the existing
  functional map.
- Existing task-016.3 behavior and TODO.md compliance remain intact.
- No dependency, lockfile, backend, API, dataset, Compose, workflow, commit, or
  push change occurs.

# Proportionate verification

Run the directly affected test only:

```bash
pnpm --dir frontend test --run src/test/GreenhouseMap.test.tsx
```

Then run:

```bash
pnpm --dir frontend typecheck
pnpm --dir frontend exec eslint \
  src/features/map/GreenhouseMap.tsx \
  src/test/GreenhouseMap.test.tsx
pnpm --dir frontend build
git diff --check
git status --short
```

Inspect the final scoped diff for lost user changes, a CSS/untransformed
background, incorrect opacity, an interactive image node, flipped coordinates,
grid/marker layering regressions, leaked image handlers, dependency changes, and
unrelated edits. Do not run backend tests, API generation, Docker, the complete
frontend suite, or runtime acceptance for this visual-only task.

# Final report

Report the copied asset path and byte size, removal of the old bed overlay,
background loading/cleanup behavior, Konva layer order, exact opacity, focused
test/typecheck/scoped-lint/build/diff-check results, documentation/roadmap changes,
and exact Git status. State that broader suites and runtime checks were
intentionally not run. Do not commit or push. Finish with `READY FOR REVIEW`,
`BLOCKED`, or `CHANGES REQUIRED`.
