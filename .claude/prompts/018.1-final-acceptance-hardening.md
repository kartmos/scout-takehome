# Goal

Close the concrete findings and incomplete runtime checks from task 018, then produce a truthful final acceptance verdict. Fix both web healthchecks, eliminate the implicit local `scout-web:latest` tag, preserve the valid README cleanup, and complete the previously skipped browser, eviction, and production-limit evidence. Do not add product features or broaden architecture.

# Verified baseline

- Candidate branch is `feat/scout-mvp`; history was rewritten and the historic `backend/api` blob is absent from reachable history.
- External recovery bundle exists outside the repository at `/home/kartmos/scout-mvp-pre-filter-20260701-161908.bundle`.
- Backend gates pass; frontend has 100/100 tests with clean typecheck/lint/build.
- Clean-clone image builds and local MinIO/API/seed/API/proxy/225-photo/singleflight checks passed.
- Dataset is tracked and unchanged: 50 JPEGs, 92 predictions, database SHA-256 `b84f73a33e99496d1152ef366d914c64a6e60cb72c494c9f2c42bc7b7dcaeb39`.
- `README.md` currently has two valid uncommitted documentation changes: dataset is now committed, and the stale task-018 limitations section was removed. Preserve them.
- Docker daemon is available.

Start with `git status --short`, branch/HEAD, and a scoped diff. Preserve all existing changes. Do not commit, push, force-push, rewrite history again, delete the recovery bundle, or remove user volumes without explicit authorization.

# Context manifest

Read only:

```text
CLAUDE.md
compose.yaml: web service only
compose.production.yaml: web service and API/web limits only
frontend/nginx.conf: listen directive only
README.md: current diff and quick-start/resource sections only
.claude/prompts/ROADMAP.md
```

Inspect logs/rendered Compose state only as needed for the checks below. Do not reread prior prompts, application source, tests, Git history, or dataset bytes. Do not use subagents.

# Finding 1 — IPv4 web healthchecks in both topologies

The unprivileged Alpine container resolves `localhost` to `::1`, while this Nginx configuration listens on IPv4 `0.0.0.0:8080`. The local container is therefore marked unhealthy even though its published IPv4 endpoint works. The same command exists in production Compose and has the same defect.

1. In both `compose.yaml` and `compose.production.yaml`, change only the web healthcheck target from `http://localhost:8080/` to `http://127.0.0.1:8080/`.
2. Preserve command form, intervals, timeouts, retries, start periods, users, filesystem restrictions, and Nginx configuration.
3. Do not change API/MinIO healthchecks that already passed live execution.

# Finding 2 — explicit non-latest local web image

The local web service has `build:` but no `image:`, and the actual acceptance build produced `scout-web:latest`, violating the no-`latest` policy.

1. Add `image: scout-web:local` to the local web service, matching the existing local API naming policy.
2. Keep production default `scout-web:0.1.0` unchanged.
3. Rebuild through Compose and prove the running local web container uses `scout-web:local`. The stale acceptance-only `scout-web:latest` image may be removed only after proving no container references it; report removal separately. Do not delete unrelated images.

# Incomplete check 1 — real web health and proxy smoke

Using a temporary untracked `.env` derived from `.env.example` with non-printed acceptance credentials:

1. Render local and production Compose and run `config --quiet`.
2. Build/start the local stack with the documented command and a bounded wait.
3. Prove MinIO, API, and web all become healthy; inspect the web health log and run the exact `wget` command inside the container as UID 101.
4. Verify the published web origin, index cache/security headers, one `/assets/` JS and CSS immutable response, SPA fallback, `/api/healthz`, and `/api/metrics`.
5. Run seed once only if the preserved MinIO volume lacks originals; do not modify the dataset.

# Incomplete check 2 — force real cache eviction

The prior run generated nine variants totaling about 117 KB but observed zero evictions. Metric presence is not runtime eviction proof.

1. Use an isolated temporary thumbnail-cache volume and an intentionally small acceptance-only cache limit that can hold at least one generated thumbnail but not the whole variant set. Do not change production defaults or source.
2. Generate enough distinct width/quality variants of one seeded photo to exceed that limit.
3. Prove the eviction counter increases, cache bytes remain at or below the configured maximum, no temp files remain, requests stay valid, and a genuinely evicted old variant regenerates when requested again.
4. Remove only the isolated acceptance cache volume after recording sanitized evidence.

# Incomplete check 3 — run declared production limits

The prior resource table used the local API limit (`256 MiB`, `GOMEMLIMIT=200MiB`) rather than production (`384 MiB`, `GOMEMLIMIT=320MiB`). Static Compose inspection is insufficient.

1. Build/tag the candidate images as `scout-api:0.1.0` and `scout-web:0.1.0` without `latest`.
2. Keep MinIO running separately as the acceptance external S3 service, then start `compose.production.yaml` with explicit non-secret acceptance values. The production Compose model itself must remain API+web only.
3. Prove the production API and web become healthy and run as their declared non-root users with read-only filesystems.
4. Confirm effective limits: API `0.75 CPU / 384 MiB`, web `0.25 CPU / 64 MiB`, combined `1 CPU / 448 MiB`, PID limits, `GOMAXPROCS=1`, `GOMEMLIMIT=320MiB`, generation concurrency `1`.
5. Capture sanitized `docker stats --no-stream` at idle, list traffic, and a bounded thumbnail burst. Require no OOM, restart, or health loss.
6. Stop the production stack non-destructively and leave user data volumes intact.

# Incomplete check 4 — live browser acceptance

Task 018 did not perform Phase 6: bundle inspection is not a browser smoke test.

Use an available real host/in-app browser against the healthy public web origin. Do not install system packages or use sudo without explicit approval. Verify at minimum:

- gallery renders real seeded cards without console errors;
- loading/populated/empty/error recovery paths;
- class and confidence filters, reset, cursor navigation, and no stale data;
- mobile and desktop responsive layouts, real `srcset` requests, and bbox alignment;
- viewer open/close/Escape/previous/next, contained-image bbox alignment, keyboard focus restoration;
- no failed resources, nested-button warning, secret-bearing API URL, or signed-query leakage in application logs.

If no browser is available, stop with `BLOCKED`; unit tests and bundle analysis cannot replace this criterion. Do not claim `ACCEPTED`.

# Final verification

After corrections and runtime checks:

```bash
docker.exe compose --env-file <temporary-env> -f compose.yaml config --quiet
docker.exe compose --env-file <temporary-env> -f compose.production.yaml config --quiet
git diff --check
git status --short
```

Also verify:

- both rendered web healthchecks target `127.0.0.1`;
- local web image is `scout-web:local`, production web image is `scout-web:0.1.0`, and no candidate service uses `latest`;
- dataset/database hashes and SQLite integrity match the verified baseline;
- reachable history still contains no `backend/api`;
- no `.env`, build output, fixture DB, cache, screenshot with sensitive URLs, or acceptance artifact is tracked;
- the only source changes are both Compose fixes, the explicit local image tag, the preserved README cleanup, and roadmap/prompt bookkeeping.

Do not rerun the full backend/frontend language gates unless a source file unexpectedly changes. Their clean-clone results from task 018 remain valid.

# Acceptance and handoff

Report:

1. finding-to-fix mapping and changed files;
2. local/production rendered healthcheck and image tags;
3. live healthy stack/proxy evidence;
4. forced eviction limit, bytes, counter, and regeneration evidence;
5. actual production-limit container configuration and idle/list/burst stats;
6. browser viewport/interactions/console/network results;
7. dataset/history/artifact/final status checks;
8. the exact uncommitted files requiring the user's final commit authorization.

Finish with exactly one verdict:

- `READY FOR FINAL COMMIT` — every runtime/browser criterion passed and only reviewed candidate changes remain uncommitted;
- `BLOCKED` — browser, Docker, credentials, or user authorization prevents completion;
- `CHANGES REQUIRED` — a new reproducible defect remains.

Do not emit `ACCEPTED` while the working tree is dirty. After `READY FOR FINAL COMMIT`, wait for explicit user authorization to commit; then acceptance must verify the new clean HEAD before any push/publication.
